Reserved for feature-colocated modules that combine multiple components,
hooks, and local state for one specific workflow (e.g. a future
`features/emergency-request-wizard/` bundling its multi-step form UI).

Empty in Phase 0 — `components/` covers today's generic, reusable pieces;
this folder exists so feature-specific composition has a home later
without needing a restructure.
