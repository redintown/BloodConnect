import { PageShell } from "@/components/ui/PageShell";

export default function RequestBloodPage() {
  return (
    <PageShell
      title="Request blood"
      phaseNote="The full emergency request form (blood group, quantity, hospital, urgency, location) lands in Phase 3."
    >
      <p className="text-gray-600">Emergency request form placeholder.</p>
    </PageShell>
  );
}
