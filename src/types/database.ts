/**
 * Placeholder for Supabase's generated database types.
 *
 * Once the project is linked to a real Supabase project, regenerate this
 * with:
 *
 *   npx supabase gen types typescript --project-id <project-id> > src/types/database.ts
 *
 * Until then, `Database` is intentionally untyped so `createClient<Database>()`
 * in lib/supabase/{client,server}.ts compiles. Domain code should NOT import
 * from this file directly — use the hand-written types in types/domain.ts,
 * which stay stable even as the generated schema types churn.
 */
export type Database = any;
