Placeholder icon only (`icon.svg`). Before shipping to production, replace
with proper 192x192 / 512x512 PNGs (and an apple-touch-icon) for platforms
that don't support SVG manifest icons well. Tracked as a Phase 10 task.
